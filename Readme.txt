

 npm init
 npm install express --save
 npm install --save lodash
 npm install jsonq
 npm install use-strict
 npm install mocha-cli@1.0.1 -g
 npm install nodemon --save-dev

 if the processes still persist, you can force the processes to terminate by adding the /f flag:
  taskkill /f /im node.exe

dependencies
https://www.npmjs.com/package/jsonq
 npm install jsonq
